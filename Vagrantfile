# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
  # v2 configs...
  config.vm.box = "Ubuntu precise 64"
  config.vm.hostname = "konektaz-vagrant"
  config.vm.network :public_network
  config.vm.box_url = "http://files.vagrantup.com/precise64.box"

end
